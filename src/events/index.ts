import * as ready from './ready';
import * as interactionCreate from './interactionCreate';
import * as modalSubmit from './modalSubmit';

export const events = [ready, interactionCreate, modalSubmit]; 